const fs = require('node:fs')
const authenticate = require('./authCheck')

module.exports = async function updateKnowledgeBase(text, eventJson) {
    const auth = await authenticate(guildID);




};      